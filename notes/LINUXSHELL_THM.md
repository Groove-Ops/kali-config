### LINUX SHELLS ###

# Comandos utiles

 *cat /etc/shells* = Ense;a las shells disponibles en el sistema
 *chsh -s /usr/bin/(shell)* = Cambia la shell seleccionada para que sea la predeterminada permanentemente
